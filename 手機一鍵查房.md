# 手機一鍵查空房（Android）

## 為什麼要做這個

GitHub Actions 的 runner（Azure 機房 IP）被官網的 Akamai 防護擋死了 ——
TCP 通、TLS 通、HTTP 送出後 25 秒零回應。換 UA、換 HTTP 版本、換 `/sp/`
手機版路徑都一樣，是**來源 IP** 的問題，程式改不動它。

你的手機從台灣住宅網路出去就進得了官網，所以把查詢搬到手機上。

## 重要：不要做成自動排程

如果讓手機每隔幾小時自動掃幾十個頁面，你家的 IP 很可能也被標記。
**代價是真正要訂房那天你可能連官網都進不去** —— 為了監控毀掉目的。

所以刻意做成「手動按一下」：看起來就是正常人在瀏覽。
每次只查幾個重點日期，不要一次掃整個月。

## 時間限制

迪士尼是「宿泊日的 4 個月前同日 11:00 JST」滾動開賣。
查詢只對**已開賣**的日期有意義：

| 想訂的日期 | 開賣時間 |
|---|---|
| 2027/01/29 (五) | 2026/09/29 11:00 JST（台灣 10:00） |
| 2027/01/30 (六) | 2026/09/30 11:00 JST |
| 2027/01/31 (日) | 2026/10/01 11:00 JST（9/31 不存在，順延） |

在那之前按了也只會看到「未開放預約」。

---

## 讓程式去排隊（推薦）

官網忙碌時會把人送進 Queue-it 等候室。**排隊本來就該讓程式去等**——
一個程式掛在那裡等一分鐘毫無成本，是人在盯進度條才浪費。

程式已經會自動處理：遇到等候室就掛著等放行（最多 20 分鐘），
放行後**一口氣查完所有日期**——排一次隊就夠，cookie 留在同一個瀏覽器
session，後面的日期不必重排。查完發 LINE。

### 在自己的電腦上跑（macOS）

GitHub Actions 的機房 IP 被官網擋死，所以必須在你自己的網路上跑。

1. 把這個 repo 抓下來（或用既有的資料夾）
2. 雙擊 **`安裝.command`**，安裝 Playwright 與 Chromium（只需一次）
3. 在同資料夾建立 `.env`：
   ```
   LINE_CLIENT_ID=你的值
   LINE_CLIENT_SECRET=你的值
   ```
   （`.env` 已加進 `.gitignore`，不會被 commit）
4. 之後要查就雙擊 **`查空房.command`**

想只查特定日期：

```bash
./查空房.command 2027-01-30,2027-01-31
```

未開賣的日期會自動略過並告訴你何時開賣。

### 流程

```
你按一下  →  程式進等候室排隊（自己等）
          →  放行後一口氣查完所有日期
          →  LINE 通知結果（房型、價格、訂房連結）
```

你不用盯著畫面。

---

## 備案：快查頁（不用裝任何東西）

手機開這一頁，把它加到主畫面：

**https://barryhuang.github.io/BarryNotification/disney.html**

- 2027 年 1 月每一天都列出來，各有「泉鄉」「海洋」兩個按鈕，點下去直接開官網搜尋結果
- 已開賣的日期才能點，未開賣的會顯示何時開賣
- 上方有目標日期（1/29、1/30、1/31）的開賣倒數，每分鐘自動更新
- 開賣時間是頁面自己算的，不連任何伺服器，所以不會失效

這是目前**唯一確定可行**的做法：用你自己的瀏覽器開官網，沒有 IP 被擋的問題，
也不受頁面是否需要 JS 渲染影響。

缺點是一次看一個日期，而且不會自動發 LINE。要更自動的話往下看。

---

## 步驟 1：先確認手機查得到（這步同時是關鍵測試）

我無法從開發環境連上官網，所以有一件事不確定：
**搜尋結果是伺服器直接吐 HTML，還是要靠 JavaScript 才渲染得出來？**

這決定後面能不能做成「純發請求 + 自動發 LINE」。先測：

1. Play 商店安裝 **HTTP Shortcuts**（免費、開源，作者 Roland Meyer）
2. 新增一個 Shortcut：
   - **Method**: GET
   - **URL**: 用下面產生的網址（挑一個已開賣的日期）
   - **Request Headers** 加一列：
     `User-Agent` = `Mozilla/5.0 (Linux; Android 14) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Mobile Safari/537.36`
   - **Response** → Display type 選 **Plain text / Window**
3. 按下去，看回來什麼

### 怎麼判讀結果

| 看到 | 代表 | 下一步 |
|---|---|---|
| 有日文房型名稱、`円` 價格、`満室` 等字樣 | **伺服器直接吐 HTML** ✅ | 可以做成全自動：一鍵查完 + 自動發 LINE |
| 只有一堆 `<script>`、看不到中文日文內容 | 需要 JS 渲染 | 改用瀏覽器方案（Firefox + 使用者腳本），結果顯示在畫面上 |
| `temporarily busy` / 被導到 `reserve-q` | 正在排隊 | 正常，等一下再按 |
| 一直轉圈最後逾時 | 手機 IP 也被擋 | 那這條路也不通，要再想辦法 |

把看到的前幾行貼給我，我接著做。

---

## 產生查詢網址

在這個 repo 執行：

```bash
# 指定日期
python3 tokyo_disney_hotel.py --urls --dates 2027-01-10,2027-01-30,2027-01-31

# 預設：列出目前所有已開賣的日期
python3 tokyo_disney_hotel.py --urls

# 也可以指定飯店與住幾晚
python3 tokyo_disney_hotel.py --urls --dates 2027-01-30 --hotels FSH,DHM,TDH --nights 2
```

飯店代碼：

| 代碼 | 飯店 |
|---|---|
| `FSH` | 東京迪士尼海洋夢幻泉鄉夢幻飯店 |
| `DHM` | 東京迪士尼海洋觀海景大飯店 MiraCosta |
| `TDH` | 東京迪士尼樂園大飯店 |
| `DAH` | 迪士尼大使大飯店 |
| `TSH` | 東京迪士尼玩具總動員飯店 |
| `DCH` | 東京迪士尼慶典飯店 |

## 步驟 2（等步驟 1 有結果再做）

視結果做成：多個日期一次查完、整理成摘要、放上桌面一鍵執行，
需要的話再串 LINE 廣播。
